package in.co.objectOrientedProgramming;

/*****
 * Interface ***** 
 * -when all methods are abstract then interface is created 
 * -it has abstract methods and constants 
 * -it represents a role (abstract view)for a class 
 * -one interface can extend another interface using extends keywords 
 
 * -one class can implement multiple interface using implements keywords.
 */
public interface Richman {
	 public void earnMoney();
	 public void donation();
	 public void party();
	 
}
